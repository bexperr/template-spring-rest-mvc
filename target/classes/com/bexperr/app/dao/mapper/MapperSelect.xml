<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bexperr.app.dao.MapperSelect">

<!-- 

	<select id="checkParticipante" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM
		SDO_CAT_PARTICIPANTS WHERE SUD_ID = #{participante}
	</select>
	
		<select id="getAllProgramas" resultType="CatalogoVO">
		SELECT SCP_ID ID,
		SCP_NAME_PROGRAM DESCRIPCION FROM SDO_CAT_PROGRAM ORDER BY
		SCP_NAME_PROGRAM ASC
	</select>
 -->

</mapper>